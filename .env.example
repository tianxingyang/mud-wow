# Local-only defaults. Copy this file to .env before starting PostgreSQL or the server.
POSTGRES_DB=mud_wow
POSTGRES_USER=mud_wow
POSTGRES_PASSWORD=mud_wow_local
POSTGRES_PORT=5432

DATABASE_URL=postgresql://mud_wow:mud_wow_local@localhost:5432/mud_wow
# Relative paths are resolved from the repository root.
CONTENT_PATH=content/northshire-v1/manifest.json
HOST=127.0.0.1
PORT=3000
